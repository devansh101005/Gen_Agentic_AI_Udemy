spice_mix=set()

print(f"Initial spice mix id: {id(spice_mix)}")

spice_mix.add('Ginger')
spice_mix.add('Cardamon')
print(f"After spice mix id: {id(spice_mix)}")

# Both id are same although they are mutable

# Mutable and immutable means whether we are changing that thing in memory or not ...in shortv mutable and immutable ka khel memory se hota hai 











